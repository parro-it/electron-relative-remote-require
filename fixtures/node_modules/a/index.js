'use strict';

const relativeRequire = require('../../..');

try {
	require('b');
	exports.required = 'b is required';
} catch (e) {
	exports.required = 'b is not required: ' + e.stack;
}

try {
	relativeRequire('b');
	exports.remoterequired = 'b is remote required';
} catch (e) {
	exports.remoterequired = 'b is not remote required: ' + e.stack;
}
